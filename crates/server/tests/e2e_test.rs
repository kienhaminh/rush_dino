#[tokio::test]
#[ignore = "requires running provider and network stack"]
async fn e2e_smoke_test_placeholder() {
    // Placeholder for full init -> start -> chat -> stop scenario.
    // Kept ignored until deterministic mock provider wiring is added.
    assert!(true);
}
