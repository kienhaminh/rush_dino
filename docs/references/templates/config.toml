host = "127.0.0.1"
port = 28847
log_level = "info"
default_profile_id = ""
data_dir = "{home_path}"
db_path = "{db_path}"
brave_search_endpoint = "https://api.search.brave.com/res/v1/web/search"

# [security]
# Require a CLI-issued one-time login code before the browser dashboard loads
# dashboard_auth_enabled = false

# [bootstrap]
# Max characters per bootstrap file injected into system prompt (default: 20000)
# max_chars_per_file = 20000
# Max total characters for all bootstrap files combined (default: 150000)
# max_total_chars = 150000
